---
interface Props {
  title: string;
  url?: string;
  image: string;
}

const { title, url = "/", image } = Astro.props;
---

<a href={url}>
  <div class="flex flex-col items-center">
    <div
      class="fade-in-bottom bg-cover bg-center w-[35rem] h-[18rem] rounded-[40px] shadow-black/25 drop-shadow-xl"
      style={{ backgroundImage: `url('${image}')` }}
    >
    </div>
    <h2 class="w-full mt-5 text-4xl text-center underline text-neutral-700">
      {title}
    </h2>
    <p class="w-full mt-4 text-xl text-center text-neutral-500">
      <slot />
    </p>
  </div>
</a>
